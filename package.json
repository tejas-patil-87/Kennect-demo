{
  "devDependencies": {
    "cypress": "^12.17.2",
    "fs": "^0.0.1-security",
    "mochawesome": "^7.1.3",
    "mochawesome-merge": "^4.3.0",
    "mochawesome-report-generator": "^6.2.0",
    "nodemailer": "^6.9.4"
  },
  "dependencies": {
    "@faker-js/faker": "^8.0.2"
  },
  "scripts": {
    "cy:run": "npx cypress run",
    "merge-and-generate": " npx mochawesome-merge cypress/reports/mochawesome/*.json > cypress/reports/merged-report/merged-report.json && npx marge cypress/reports/merged-report/merged-report.json --reportDir ./  --inline",
    "final": "npx cypress run && merge-and-generate"
  }
}
